# EdgeTools — зависимости
# Проверено: Python 3.11.5, Windows 10/11 (июнь 2026)
#
# Установка:
#   pip install -r requirements.txt
#
# PyTorch с CUDA 12.1 — см. блок в конце файла (или README).

# UI и система
PySide6==6.10.2
pywebview==6.1
pywin32==311

# Изображения
Pillow==12.2.0
opencv-python==4.6.0.66
numpy==2.2.6
scikit-image==0.26.0

# ML (без torch — отдельный индекс ниже)
onnxruntime==1.24.4
mediapipe==0.10.33
basicsr==1.4.2
facexlib==0.3.0

# OCR
pytesseract==0.3.13

# Медиа
python-mpv==1.0.8
yt-dlp==2026.3.17

# Сеть
requests==2.32.5

# Система
psutil==7.2.2
setproctitle==1.3.7

# --- PyTorch (CUDA 12.1) — раскомментируйте при GPU NVIDIA ---
# pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
#
# Или CPU-only:
# pip install torch==2.5.1 torchvision==0.20.1
